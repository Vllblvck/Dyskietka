# List containing tuples with a path to a file and google drive directory id
# Paths can contain patterns according to the rules used by UNIX shell
# Use 'root' for top directory
BACKUPS = [('/example/path/to/example/file', 'invalidDirId'),
           ('/example/path/to/example/file2', 'invalidDirId2')]
